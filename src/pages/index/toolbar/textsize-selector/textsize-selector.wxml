<view class="textsize-selector-container">
  <view class="silder">
    <view class="silder-text">正常大小</view>
    <view 
      wx:for="{{sizeList}}"
      wx:key="item"
      class="silder-block {{index === 2 ? 'action' : ''}}">
    </view>
  </view>
</view>